// Imports
// ------------

// Exports
// ------------
export interface StatementProps {
    text?: string;
    columnOverride?: number;
    wrapperRef?: React.RefObject<HTMLElement | null>;
    isActive?: boolean;
}